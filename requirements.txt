# Runtime dependencies for the GNSS adversarial-robustness pipeline (Paper 1).
# Python 3.14. Pinned to the versions used to produce the reported results.
#
# Only packages the code actually imports are listed. The previous file was a
# TensorFlow/Keras export and did not reflect this codebase, which is PyTorch based.
# The decision-based boundary attack (experiments/13_blackbox_attacks.py) is a custom
# vectorized search, so neither ART nor cleverhans is required at runtime.
#
# KAGGLE / GPU NOTE: do NOT `pip install -r requirements.txt` on Kaggle. Its image
# already ships a compatible CUDA torch/scikit-learn/xgboost/lightgbm stack, and these
# exact pins (torch CPU build, numpy 2.4, pandas 3.0) can break it. See KAGGLE_RUNBOOK.md.
# This file targets local reproduction. torch 2.12.0 was installed as the CPU wheel
# (2.12.0+cpu); install the CPU or CUDA build appropriate to the host, the version is
# what matters for reproducibility.

# --- core numerics / data ---
numpy==2.4.4
pandas==3.0.3
scipy==1.17.1

# --- classical machine learning ---
scikit-learn==1.8.0
xgboost==3.2.0
lightgbm==4.6.0
imbalanced-learn==0.14.1
joblib==1.5.3

# --- deep learning (PyTorch) ---
torch==2.12.0

# --- figures ---
matplotlib==3.10.9
seaborn==0.13.2
Pillow==12.2.0
